<!DOCTYPE html>
<html lang="en">
  <head>
    <%= render 'shared/page_header' %>
    <%= themeable_stylesheet_link_tag :theme %>
    <title><%=h page_title %></title>
  </head>

  <body>
    <div class="container">
      <div class="header">
        <% if @frontpage -%>
          <h1 id="sitename">matijs.net</h1>
        <% else -%>
          <h1 id="sitename"><%= link_to this_blog.blog_name, this_blog.base_url %></h1>
          <p class="desc"><%= this_blog.blog_subtitle %></p>
        <% end -%>
      </div>

      <div class="content">
        <%= yield %>
      </div>

      <div class="sidebar">
        <% if @frontpage -%>
          <%= render :partial => "articles/frontbar" %>
        <% else -%>
          <p>Toxic Elephant is <a href="/">Matijs van Zuijlen</a>'s weblog.</p>
          <%= t(".powered_by", powered_by: link_to('Publify', 'http://publify.co')) %>

          <!-- sidebar components -->
          <%= render_sidebars %>
        <% end -%>
      </div>

      <div class="footer">
        <p class="copyright">
        Copyright 2002&ndash;2014, Matijs van Zuijlen
        </p>
      </div>
    </div>
  </body>
</html>
